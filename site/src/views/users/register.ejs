<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head',{title:'XL - Registro'}) %>

    <body class="register">
        <%- include('../partials/header') %>
            <main>
                <h1>Registrate</h1>
                <%- include('../partials/breadcrumbs',{página:'Registrate'}) %>
                    <section class="card">
                        <form class="form-register" action="" method="POST">
                            <section class="card-header">
                                <h2>¡Sé parte de nuestra web y podrás disfrutar de todos nuestros productos!</h2>
                                <p>¡Es muy fácil y sólo te llevará unos minutos!</p>
                                <p>Cualquier duda, consulta o problema consultarlo en el formulario de <a
                                        href="/#contacto">Contacto</a>.</p>
                            </section>

                            <section class="card-body">
                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="name">Nombre</label>
                                        <input type="text" name="name" id="name" placeholder="Ingresa tu nombre" value="<%= locals.old && old.name ? old.name : null %>">
                                        <% if (locals.errors && errors.name) {%>
                                        <small><%= errors.name.msg %></small>
                                        <% } %> 
                                    </div>  
                                    <div class="form-group">
                                        <label for="lastname">Apellido</label>
                                        <input type="text" name="lastname" id="lastname" placeholder="Ingresa tu apellido" value="<%= locals.old && old.name ? old.lastname : null %>">
                                        <% if (locals.errors && errors.lastname) {%> 
                                            <small><%= errors.lastname.msg %></small>
                                        <% } %> 
                                    </div>
                                </section>

                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="email">Email</label>
                                        <input type="email" name="email" id="email" placeholder="Ingresa tu email" value="<%= locals.old && old.name ? old.email : null %>">
                                        <% if (locals.errors && errors.email) {%> 
                                            <small><%= errors.email.msg %></small>
                                        <% } %> 
                                    </div>
                                    <div class="form-group">
                                        <label for="phone">Teléfono</label>
                                        <input type="tel" name="phonenumber" id="phone" value="<%= locals.old && old.name ? old.phonenumber : null %>">
                                        <!-- <div class="verify">
                                            <button type="submit" class="button">Verificar</button>
                                            <div class="alert-msg" style="display: none;"></div>
                                        </div> -->
                                        <% if (locals.errors && errors.phonenumber) {%> 
                                            <small><%= errors.phonenumber.msg %></small>
                                        <% } %>                                            
                                    </div>
                                </section>

                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="pass">Clave</label>
                                        <input type="password" name="pass" id="pass" placeholder="Password">
                                        <% if (locals.errors && errors.pass) {%> 
                                            <small><%= errors.pass.msg %></small>
                                        <% } %>                                            
                                    </div>
                                    <div class="form-group">
                                        <label for="pass2">Repetir Clave</label>
                                        <input type="password" name="pass2" id="pass2" placeholder="Password">
                                        <% if (locals.errors && errors.pass2) {%> 
                                            <small><%= errors.pass2.msg %></small>
                                        <% } %>                                            
                                    </div>
                                </section>

                            </section>
                            <section class="card-footer">
                                <div class="register-button">
                                    <button type="submit"><i class="fas fa-user-plus"></i> ¡Registrarme!</button>
                                </div>
                                <div class="inicia-sesión">
                                    <h4>¿Ya estás registrado?
                                        <a href="/users/login">Inicia sesión aquí</a>
                                    </h4>
                                </div>
                            </section>
                        </form>
                    </section>
            </main>
            <%- include('../partials/footer') %>
    </body>

    <script>
        const phoneInputField = document.querySelector("#phone");
        const phoneInput = window.intlTelInput(phoneInputField, {
            preferredCountries: ["ar", "cl", "uy", "ec", "co", "ce", "pe", "py", "bo", "br", "ve", "mx", "us", "ca", "es"],
            utilsScript: "https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.18/js/utils.js"
        });
/*         const info = document.querySelector(".alert-msg");

        function process(event) {
            event.preventDefault();

            const phoneNumber = phoneInput.getNumber();

            info.style.display = "";
            info.innerHTML = `Su número de teléfono es: <strong>${phoneNumber}</strong>`;
        } */
    </script>

</html>