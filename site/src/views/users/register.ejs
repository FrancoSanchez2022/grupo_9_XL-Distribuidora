<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head',{title:'XL - Registro'}) %>

    <body class="register">
        <%- include('../partials/header') %>
            <main>
                <h1>Registrate</h1>
                <%- include('../partials/breadcrumbs',{página:'Registrate'}) %>
                    <section class="card">
                        <form class="form-register" action="/users/register" method="POST" id="formulario">
                            <section class="card-header">
                                <h2>¡Sé parte de nuestra web y podrás disfrutar de todos nuestros productos!</h2>
                                <p>¡Es muy fácil y sólo te llevará unos minutos!</p>
                                <p>Cualquier duda, consulta o problema consultarlo en el formulario de <a
                                        href="/#contacto">Contacto</a>.</p>
                            </section>

                            <section class="card-body">
                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="name">Nombre</label>
                                        <input type="text" name="name" id="name" placeholder="Ingresa tu nombre"
                                            value="<%= locals.old && old.name ? old.name : null %>">
                                            <small id="nameContainer">
                                                <%= locals.errors && errors.name ? errors.name.msg : null %>
                                            </small>
                                          
                                    </div>
                                    <div class="form-group">
                                        <label for="lastname">Apellido</label>
                                        <input type="text" name="lastname" id="lastname"
                                            placeholder="Ingresa tu apellido"
                                            value="<%= locals.old && old.lastname ? old.lastname : null %>">
                                            <small id="lastnameContainer">
                                                <%= locals.errors && errors.lastname ? errors.lastname.msg : null %>
                                            </small>
                                    </div>
                                </section>

                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="email">Email</label>
                                        <input type="email" name="email" id="email" placeholder="Ingresa tu email"
                                            value="<%= locals.old && old.email ? old.email : null %>">
                                            <small id="emailContainer">
                                                <%= locals.errors && errors.email ? errors.email.msg : null %>
                                            </small>
                                    </div>
                                    <div class="form-group">
                                        <label for="phone">Teléfono</label>
                                        <input type="tel" name="phonenumber" id="phone"
                                            value="<%= locals.old && old.phonenumber ? old.phonenumber : null %>">

                                        <!-- <div class="verify">
                                            <button type="submit" class="button">Verificar</button>
                                            <div class="alert-msg" style="display: none;"></div>
                                        </div> -->

                                            <small id="phonenumberContainer">
                                                <%= locals.errors && errors.phonenumber ? errors.phonenumber.msg : null %>
                                            </small>
                                    </div>
                                </section>

                                <section class="container bottom">
                                    <div class="form-group">
                                        <label for="pass">Clave</label>
                                        <div class="password">
                                            <input type="password" name="pass" id="pass" placeholder="Password">
                                            <i class="fas fa-eye-slash" id="eye-pass"></i>
                                        </div>
                                            <small id="passContainer">
                                                <%= locals.errors && errors.pass ? errors.pass.msg : null %>
                                            </small>
                                    </div>
                                    <div class="form-group">
                                        <label for="pass2">Repetir Clave</label>
                                        <div class="password">
                                            <input type="password" name="pass2" id="pass2" placeholder="Password">
                                            <i class="fas fa-eye-slash" id="eye-pass2"></i>
                                        </div>
                                            <small id="passContainer2">
                                                <%= locals.errors && errors.pass2 ? errors.pass2.msg : null %>
                                            </small>
                                    </div>
                                </section>

                            </section>
                            <section class="card-footer">
                                <div class="register-button">
                                    <button type="submit"><i class="fas fa-user-plus"></i> ¡Registrarme!</button>
                                </div>
                                <div class="inicia-sesión">
                                    <h4>¿Ya estás registrado?
                                        <a href="/users/login">Inicia sesión aquí</a>
                                    </h4>
                                </div>
                            </section>
                        </form>
                    </section>
            </main>
            <%- include('../partials/footer') %>
                <script src="/javascripts/users/register.js"></script>
                <script src="/javascripts/users/phone.js"></script>
                <script src="/javascripts/users/eyePass.js"></script>
    </body>

</html>